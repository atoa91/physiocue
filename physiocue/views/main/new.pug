extends ../base


block content
    h1 New measure

    form(action="/api/measure/" method="POST")
        input(type="number" name="SYS" placeholder="SYS" required)
        input(type="number" name="DIA" placeholder="DIA" required)
        input(type="number" name="pulse" placeholder="pulse" required)
        input(type="number" name="statuss" placeholder="status" required)
        
        input(type="submit" value="submit" required)
